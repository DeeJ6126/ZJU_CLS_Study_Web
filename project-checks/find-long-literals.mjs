// Diagnostic helper used during the audit follow-up. Kept as a no-op stub
// because mavis-trash is unavailable in this environment; re-introduce the
// implementation locally if a future content audit needs it.
export const findLongLiterals = () => {
  throw new Error('find-long-literals.mjs is a stub; implementation removed.');
};
